# 参考业务补齐执行记录

设计：docs/reference-alignment-design.md

## 状态

- 用户已批准执行设计，包括千分比返水、躲避正净赢基数、旧单保留旧机制、新单快照。
- 前序改动已提交为 5cf7f44，当前分支 codex/reference-business-alignment。
- 用户已要求先提交现有改动，再在本地功能分支执行；不创建额外工作区，不推送或部署。
- rebate配置、快照、发放、分页明细及分币种统计均已接入；本人排名、后台作废、普通玩家创建及绑定上级已接入本地。
- 审批已恢复，0060在本地测试库执行成功，初始化144条；配置持久化、权限、版本及审计测试通过。
- 0061已在本地测试库执行；实际PlaceBet→并发SettleRound→重复结算验证通过：下单后代理升级仍使用旧快照，上下路本金/躲避正净赢分别计算，明细和账本不重复。另覆盖余额溢出回滚后重试。
- 本批验证：带本地POSTGRES_TEST_DSN的 `go test -p 1 ./... -count=1` 通过；rebate/settlement两包 `go test -race` 通过；`go vet ./...` 与 `git diff --check` 通过。未提交、未推送、未部署。

## 批次

1. 代理返水：计算/配置、快照、结算发放、本人/后台明细及统计已实现；旧佣金更新响应明确legacy_bets_only。
2. 本人排名：已实现，列表外self、同快照、币种/周期隔离、他人余额裁剪。
3. 后台单笔作废：0062，已实现权限/二级密码、幂等退款、模拟隔离、作废明细及统一退款查询；公共事件只含投注/轮次/状态，不广播操作人或原因。
4. 普通玩家创建、后台绑定上级：已实现；零余额player创建，真实关系绑定防环、禁止改挂、维护后代路径；玩家绑定与注册共用图锁。
5. 白名单：0063配置管理已实现。用户明确确认暂不接第三方，risk_check_enabled=false、whitelist_effective=false；没有接入或请求外部IP风险服务，不改变现有身份验证。

## 最终验收（本地）

- 新建独立本地验收数据库，按顺序应用0001–0063全部迁移通过；没有删除原测试库。
- `POSTGRES_TEST_DSN=本地独立验收库 go test -p 1 ./... -count=1` 全包通过。
- OpenAPI YAML解析及全部本地$ref检查通过；HTTP路由契约、金额与中文错误测试通过。
- 最终 `go test -race -p 1` 验证rebate、settlement、agent、operations和httpapi五包通过；`go vet ./...`、`git diff --check`及OpenAPI重复键检查通过。
- 交付留在codex/reference-business-alignment，未提交、未推送、未部署。
- 工作期间发现其他来源的用户代理等级0支持修改，保持不覆盖；不将其归为本批新增功能。

## 已核实的实施注意点

### 0064 哈希同条件合单

- 已接入下单时合单，不是列表临时求和；真实/虚拟共享合单规则，币种和选项隔离，保留逐次幂等记录、扣款流水及增量事件。
- 本人/公开/后台查询返回合计，撤单与结算处理整单。稳定bet_id、首单赔率/返水/时间保留，last_placed_at单独记录追加；历史订单不追溯合并。与参考删除重建ID及更新订单时间的技术差异已在前端文档说明。
- 独立验收库顺序应用0001–0064通过，带POSTGRES_TEST_DSN的全项目测试通过。新增真实数据库用例验证三次10分合30分、参数冲突、跨币种/选项隔离、六路并发重试、整单退款、取消后重新下单、整单派奖取整、模拟输赢与取消不产生资金流水。
- 只读审查发现的历史额外selection字段原样重试问题已先复现后修复；保留完整请求JSON，仅合单查询按pick匹配。OpenAPI解析/引用、go vet及diff检查通过。未提交或部署。

- 当前下单 SQL 位于 internal/application/betting/service.go，写入 bets 时必须同时保存返水版本及规则/代理链快照。
- 当前结算 SELECT 同时锁投注和钱包；随后再锁代理钱包。多级返水不能直接在该循环内追加不排序的钱包锁，必须先整理本期所有收款钱包并统一加锁。
- 旧 applyCommission 在中奖判定前执行；新躲避返水必须在计算 payout 后使用 max(payout-stake,0)，不能复用当前调用位置。
- 当前排行榜 HTTP 层对其他玩家余额做裁剪；新增 self 必须保持该隐私边界，且不能只从返回的 items 查本人。
- 参考白名单执行入口是 src/LoginIPValidation.ts，不能只复制 GM 管理接口而忽略登录时校验用途。
- 只允许本地修改；远程操作仍受 AGENTS.md 唯一授权目标规则约束，本次未进行远程操作。
